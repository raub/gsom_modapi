extends IGsomEntity
class_name GsomPassiveReplicator

# Passive network entity used for scene-only content that has no custom replicator.
# It exists so static world scenes can still be spawned/despawned across clients.
